# Add directories or file patterns to ignore during indexing (e.g. foo/ or *.csv)

# Ignore build directory except build/conf
# Explicitly ignore build subdirectories and files
build/tmp/
build/cache/
build/downloads/
build/sstate-cache/
build/*.log
# Keep build/conf directory
